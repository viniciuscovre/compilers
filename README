TO DO
- FLOAT operations - juntar com versão com IFs
- verificação is_available para ver se tem posição disponível na symtab
- colocar as labels(T_EXPR, T_TERM,....)
- adicionar codigo de IF ELSE WHILE .....
- Reconhecer mais constantes (não apenas a primeia cte digitada) quando usado o comando ENTER (nao ';')
- colocar no vmachine "<" ">"
- Operadores: Diferente, igual, maior, menor, and, or, not...etc
- No compilador, comando é uma sentença(todo comando é uma sentença, nem toda sentença é um comando)
// em keywords.[ch]
// begin if then else while do repeat until procedure function end
- Parser - na hora de declarar -> botar na tabela
- Em expr() -> case ID -> variables() -> verificar se o simbolo foi devidamente cadastrado, retornar erro
- toda consulta verificar existencia do simbolo

DONE
- colocar iskeyword na isidentifier - DONE - Yuji
- implementar Float ~vina
- implementar Hex, Oct FEITO ~vina
- verificar \n no skipspace FEITO ~William
- alterar o nome do projeto de mypas para progcalc ~FEITO ~William (Assumindo que a tarefa correta seja mudar o nome de progcalc para mypas)
- cmdsep deve ter os casos de ";" e "/n" FEITO ~William
- token mismatch quando acaba com ";" FEITO ~William
- - na função gettoken - alterar "tokenstream" para "sourcecode" FEITO -William
